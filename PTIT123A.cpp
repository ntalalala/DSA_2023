#include<bits/stdc++.h>
using namespace std;
int main(){
	while(true){
		string s;
		getline(cin, s);
		int k = 0, z = 0;
		vector <pair <string, int>> v;
		while (s.find(",") != string :: npos){
			int h = s.find(",")
			v.push_back(s.substr())
		}
	}
}
